import { CalculateDistancePipe } from './calculate-distance.pipe';

describe('CalculateDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
