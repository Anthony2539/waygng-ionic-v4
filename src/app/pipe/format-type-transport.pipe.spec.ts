import { FormatTypeTransportPipe } from './format-type-transport.pipe';

describe('FormatTypeTransportPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTypeTransportPipe();
    expect(pipe).toBeTruthy();
  });
});
